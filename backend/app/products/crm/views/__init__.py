"""Saved list views: a named set of filters, columns and ordering.

A view is a *bookmark for a question*, not a copy of its answer. It stores
what to ask — filters, sort, which columns to show — and never any rows, so a
view opened tomorrow shows tomorrow's records and can never go stale.
"""

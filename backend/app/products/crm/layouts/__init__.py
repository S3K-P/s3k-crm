"""Record layouts: the admin form/layout builder (Checkpoint 4).

A layout arranges an entity type's built-in and tenant-defined fields into
labelled sections and columns, and may attach conditional rules that show,
hide or require a custom field based on the record's other values. See
:mod:`.models` for the persisted shape, :mod:`.evaluate` for how a rule is
decided, and :mod:`.service` for what publishing means.
"""

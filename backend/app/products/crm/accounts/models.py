"""SQLAlchemy models for the accounts module.

Placeholder — no tables are defined yet. Database models arrive with the
tenant-context and RLS task; when they do, this module must be imported from
``app.core.metadata`` so Alembic autogenerate can see it.
"""

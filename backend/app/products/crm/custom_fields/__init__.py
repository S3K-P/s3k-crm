"""Tenant-defined fields and the option lists they choose from.

One module for both, for the reason ``emails`` is one module for messages and
templates: a picklist exists to be the option set of a custom field, and an
administrator who may define a field but not the list it draws from is a role
nobody would deliberately create.
"""

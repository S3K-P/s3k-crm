"""Domain events for the custom fields module.

Placeholder. Configuration changes are recorded in the audit trail rather than
published: nothing in the product reacts to a field being defined, and an
outbox event nobody consumes is a queue that only ever grows.
"""

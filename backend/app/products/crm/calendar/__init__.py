"""A read model over the things in the CRM that happen at a time.

Not a table. A calendar entry is a meeting or a task the user already has,
projected into one shape so a month grid can render both — see :mod:`.service`
for why this is a read model rather than a fourth kind of record.
"""

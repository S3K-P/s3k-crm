"""S3K CRM module: campaigns.

Marketing campaigns and their members (doc 05 "Campaign").

The tables and their ORM models exist and are covered by RLS; the service,
repository, schemas and router are not implemented yet, so no campaign HTTP
endpoint is mounted. The seven-file shape is kept so the module matches every
other one when it is completed.
"""
